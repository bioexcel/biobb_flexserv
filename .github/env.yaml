name: test_environment
channels:
  - conda-forge
  - bioconda
  - anaconda
dependencies:
  - biobb_common ==5.2.2
  - perl
  - flexserv ==1.0.2
  - pcasuite ==1.0.0=h7baada4_6
  # To avoid fail related to old CPU architecture of the self-host runner
  - numpy <2.4.0 

